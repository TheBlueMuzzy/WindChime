---
phase: 01-project-foundation
plan: 01
type: execute
---

<objective>
Scaffold the Vite + React + TypeScript project with all known dependencies installed and a running dev server.

Purpose: Establish the foundation that every subsequent phase builds on — project structure, tooling, and dependency baseline.
Output: A working Vite dev server at localhost:5173 with React + TypeScript, all project dependencies installed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Tech stack:** React + TypeScript + Vite (Muzzy's standard web game stack)
**No prior phases — this is the first.**

**All project dependencies (from roadmap/PROJECT.md):**
- @yudiel/react-qr-scanner (Phase 2 — install now to avoid later churn)
- barcode-detector polyfill (Phase 2 — companion to scanner)
- No other external deps — Web Audio API is browser-native
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite + React + TypeScript project</name>
  <files>package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/vite-env.d.ts</files>
  <action>Run `npm create vite@latest . -- --template react-ts` in the WindChime project root. This creates the standard Vite scaffold. After creation, delete the default CSS files (src/App.css, src/index.css) and strip the boilerplate JSX from App.tsx — replace with a minimal `<div>WindChime</div>` placeholder. Keep main.tsx clean (no CSS import). Do NOT delete src/assets/ — Vite needs it for static asset handling.</action>
  <verify>Files exist: package.json, vite.config.ts, src/main.tsx, src/App.tsx. `cat package.json` shows react and typescript in dependencies/devDependencies.</verify>
  <done>Vite project scaffolded with React + TypeScript template, boilerplate CSS removed, App.tsx has minimal placeholder content.</done>
</task>

<task type="auto">
  <name>Task 2: Install all project dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>Run `npm install` to install the base Vite/React deps. Then install project-specific dependencies: `npm install @yudiel/react-qr-scanner barcode-detector`. The scanner lib is needed in Phase 2 but installing now avoids a separate install step later. barcode-detector is its polyfill companion for iOS Safari. Do NOT install any other packages — Web Audio API is browser-native, no library needed.</action>
  <verify>`npm ls @yudiel/react-qr-scanner barcode-detector` shows both installed. `npm ls react react-dom typescript` shows core deps.</verify>
  <done>All dependencies installed: react, react-dom, typescript, @yudiel/react-qr-scanner, barcode-detector. No errors in npm install output.</done>
</task>

<task type="auto">
  <name>Task 3: Verify dev server starts</name>
  <files>None (verification only)</files>
  <action>Run `npm run dev` briefly to verify the dev server starts on port 5173. Check that vite outputs "Local: http://localhost:5173/" with no errors. Kill the process after confirming. Also run `npx tsc --noEmit` to verify TypeScript compiles cleanly.</action>
  <verify>`npm run dev` outputs "Local: http://localhost:5173/" with no errors. `npx tsc --noEmit` exits with code 0.</verify>
  <done>Dev server starts cleanly, TypeScript compiles without errors, project is ready for development.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npx tsc --noEmit` passes
- [ ] package.json contains react, react-dom, typescript, @yudiel/react-qr-scanner, barcode-detector
- [ ] src/App.tsx exists with minimal placeholder content
- [ ] No default Vite boilerplate CSS files remain
</verification>

<success_criteria>

- All 3 tasks completed
- Dev server runs cleanly on localhost:5173
- TypeScript compiles without errors
- All project dependencies installed and listed in package.json
- Clean project structure ready for app shell (Plan 01-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
