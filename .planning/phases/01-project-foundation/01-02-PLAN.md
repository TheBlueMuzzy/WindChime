---
phase: 01-project-foundation
plan: 02
type: execute
---

<objective>
Create a full-screen dark app shell with a camera placeholder element, optimized for landscape phone viewing.

Purpose: Establish the visual container that the camera feed and QR detection will render into. The layout must work in landscape on mobile phones.
Output: A dark, full-screen app shell with a centered "camera placeholder" area, responsive for landscape mobile.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md

**From PROJECT.md constraints:**
- Camera: Rear-facing camera in landscape orientation
- Pretty UI is out of scope — functional over beautiful
- Full-screen, no chrome, dark background

**Established patterns (from Plan 01-01):**
- Vite + React + TS project structure
- Dependencies installed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global styles and viewport setup</name>
  <files>src/styles.css, index.html</files>
  <action>Create `src/styles.css` with global reset: margin/padding 0, box-sizing border-box, html/body at 100dvh/100dvw, overflow hidden, background #111, color #fff, font-family system-ui. Add `<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">` to index.html head if not already there — prevents zoom on mobile which breaks camera UX. Import styles.css in main.tsx.</action>
  <verify>Dev server shows dark full-screen page with no scrollbars, no white borders.</verify>
  <done>Global styles applied: dark background, full viewport coverage, no scroll, no zoom on mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Build App shell with camera placeholder</name>
  <files>src/App.tsx</files>
  <action>Replace App.tsx content with a full-screen container using inline styles (no CSS modules needed for this simple layout). Structure: outer div fills viewport (100dvw x 100dvh, display flex, align/justify center, background #111). Inner div acts as camera placeholder — 80% width, 60% height, background #222, border 2px dashed #444, border-radius 8px, display flex, align/justify center. Text inside: "Camera will appear here" in #666. This is a placeholder — Phase 2 replaces the inner div with the actual scanner component. Use descriptive variable names, no abstractions.</action>
  <verify>`npm run dev` shows centered dashed-border rectangle on dark background. `npx tsc --noEmit` passes.</verify>
  <done>App shell renders: dark full-screen background, centered camera placeholder with dashed border, "Camera will appear here" text. Responsive to viewport size.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npx tsc --noEmit` passes
- [ ] Page shows dark full-screen background with no scrollbars
- [ ] Centered camera placeholder visible with dashed border
- [ ] No zoom/scroll behavior on mobile viewport
</verification>

<success_criteria>

- All tasks completed
- App shell renders correctly
- TypeScript compiles without errors
- Layout is responsive and fills viewport
- Phase 1 complete — ready for Phase 2 (QR Scanner Integration)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 2: App Shell Layout Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Phase Readiness

Phase 1 complete. Ready for Phase 2: QR Scanner Integration.
</output>
